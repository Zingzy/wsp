{
  "name": "@wsp/catalog",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run packages/catalog"
  },
  "dependencies": {
    "@wsp/protocol": "workspace:*"
  },
  "license": "AGPL-3.0-only"
}
