{
  "name": "@wsp/collect",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts src/live.ts --format esm --dts",
    "test": "vitest run",
    "live": "tsup src/index.ts src/live.ts --format esm --dts --silent && node dist/live.js"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.20.1"
  },
  "license": "AGPL-3.0-only"
}
